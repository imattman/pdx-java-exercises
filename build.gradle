/*
 *
 */

 subprojects {   // can instead use 'allProjects' to apply globally
   apply plugin: 'java'

   repositories {
     mavenCentral()
     jcenter()
   }

  dependencies {
    compile 'org.slf4j:slf4j-api:1.7.21'
    //compile 'log4j:log4j:1.2.17'

    testCompile 'junit:junit:4.12'
  }
}
